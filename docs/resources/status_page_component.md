---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "uptimepage_status_page_component Resource - uptimepage"
subcategory: ""
description: |-
  Curates one monitor onto a status page, with optional per-page overrides. Changing the page or the target replaces the resource.
---

# uptimepage_status_page_component (Resource)

Curates one monitor onto a status page, with optional per-page overrides. Changing the page or the target replaces the resource.

## Example Usage

```terraform
resource "uptimepage_target" "api" {
  name     = "api"
  interval = 60
  check = {
    type = "http"
    http = {
      url             = "https://example.com/healthz"
      expected_status = { kind = "exact", exact = 200 }
    }
  }
}

resource "uptimepage_status_page" "public" {
  slug = "acme"
  name = "Acme Status"
}

# Curate the monitor onto the page, with per-page overrides.
resource "uptimepage_status_page_component" "api" {
  status_page_id = uptimepage_status_page.public.id
  target_id      = uptimepage_target.api.id

  public_name  = "API"
  public_group = "Core services"
  sort_order   = 0
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `status_page_id` (String) Id of the status page (UUID).
- `target_id` (String) Id of the monitor to curate (UUID).

### Optional

- `public_description` (String) Per-page description shown under the component.
- `public_group` (String) Group heading the component renders under.
- `public_name` (String) Per-page display name. Falls back to the monitor name when unset.
- `sort_order` (Number) Order within the group (ascending).

### Read-Only

- `id` (String) Composite id, `status_page_id:target_id`.
- `monitor_name` (String) The monitor's operator-side name (read-only).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Components are imported by "status_page_id:target_id".
terraform import uptimepage_status_page_component.api 0192f3a4-5b6c-7d8e-9f01-23456789abcd:0192aaaa-bbbb-cccc-dddd-eeeeeeeeeeee
```
